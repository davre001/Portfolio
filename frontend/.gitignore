# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/

# Build / deps (if a bundler is added later)
node_modules/
dist/
.cache/

# Don't commit large raw media; keep compressed/optimized only
# (uncomment if you want to exclude source footage)
# assets/videos/**/*.mov
# assets/videos/**/*.prproj

.vercel
.env*
